#include <stdio.h>
int main()
{
    int l, c, duracao;
    duracao = 0;
    scanf("%d %d", &l, &c);
    while (duracao<l){
        for (int i = 0; i < c; i++)
        {
            
        }
        
    }

    
    
    return 0;
}
